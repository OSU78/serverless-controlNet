name: Build and Push Docker Image

on:
  push:
    branches:
      - master  # déclenche le workflow à chaque push sur la branche main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to Docker Hub
        uses: docker/login-action@v1
        with:
          username: "osu78"
          password: "dckr_pat_WMDWX6YD9UZDIee7q6GwdxjTPfs"

      - name: Build and Push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: osu78/serverless_controlnet:controlnet1
          build-args: 
            BASE_IMAGE=nvidia/cuda:11.6.2-cudnn8-devel-ubuntu20.04
